---
title: Quickstart
description: Send your first event into Sankofa with the Flutter or Web SDK, then verify the path from client to engine.
section: getting-started
order: 2
toc: true
badge: Official
source_of_truth:
  - sdk/sankofa_flutter/README.md
  - sdk/web/packages/browser/src/index.ts
---

## 1. Point the SDK at your engine

Sankofa SDKs accept several endpoint forms for local and production setups:

- a server base URL: `https://api.sankofa.dev`
- an API base URL: `https://api.sankofa.dev/api/v1`
- the full ingest URL: `https://api.sankofa.dev/api/v1/track`

The SDKs normalize these inputs internally.

## 2. Initialize the SDK

<CodeTabs
  tabs={[
    {
      label: "Flutter",
      code: `void main() async {
  WidgetsFlutterBinding.ensureInitialized();
  await Sankofa.instance.init(
    apiKey: 'sk_live_12345',
    endpoint: 'https://api.sankofa.dev',
    debug: true
  );
  runApp(MyApp());
}`,
    },
    {
      label: "Web (NPM)",
      code: `import { Sankofa } from "@sankofa/browser";

await Sankofa.init({
  apiKey: "sk_live_12345",
  endpoint: "https://api.sankofa.dev",
});`,
    },
    {
      label: "Web (CDN)",
      code: `<script src="https://cdn.sankofa.dev/sdk/web/v1/sankofa.min.js"></script>
<script>
  Sankofa.init({
    apiKey: "sk_live_12345",
    endpoint: "https://api.sankofa.dev"
  });
</script>`,
    },
  ]}
/>

## 3. Track the first event

<CodeTabs
  tabs={[
    {
      label: "Flutter",
      code: `await Sankofa.instance.track('button_clicked', {
  'bg_color': 'blue',
  'screen': 'home',
});`,
    },
    {
      label: "Web",
      code: `Sankofa.track("button_clicked", {
  bg_color: "blue",
  screen: "home",
});`,
    },
  ]}
/>

## 4. Identify when the user is known

<CodeTabs
  tabs={[
    {
      label: "Flutter",
      code: `await Sankofa.instance.identify('user_123');`,
    },
    {
      label: "Web",
      code: `await Sankofa.identify("user_123");`,
    },
  ]}
/>

## 5. Verify the ingest path

<StepList
  steps={[
    {
      title: "Start the engine",
      body: "Run the Sankofa engine and ensure your application can reach the configured endpoint.",
    },
    {
      title: "Use a valid API key",
      body: "The engine rejects missing or invalid x-api-key values. Use the project live or test key you generated in the dashboard.",
    },
    {
      title: "Watch the dashboard",
      body: "After the first track call succeeds, the event should appear in the 'Events' or 'Insights' section of your project.",
    },
  ]}
/>

<Callout title="SDK Normalization" tone="info">
  Sankofa SDKs normalize endpoints internally. This allows you to pass the same
  base path regardless of whether you are in a local development or production
  environment.
</Callout>

## Direct transport model

The SDKs write one JSON payload per queued transport item:

- `track` events go to `POST /api/v1/track`
- `people` updates go to `POST /api/v1/people`
- `alias` events go to `POST /api/v1/alias`

If you are not using our official SDKs, see the
[HTTP ingestion guide](/integrations/http-ingestion).

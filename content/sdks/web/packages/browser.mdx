---
title: "@sankofa/browser"
description: The core web SDK package. init, track, screen, identify, peopleSet/setPerson, reset, flush, shutdown — plus snapshot and transport status accessors.
badge: Core
toc: true
sdks: [web]
---

`@sankofa/browser` is the heart of the web SDK. It manages the queue, owns the session, exposes the identity APIs, and registers the plugins from the other packages. Every web app integrating Sankofa installs this one.

## Install

<CodeBlock lang="bash" code={`npm install @sankofa/browser`} />

## `Sankofa.init(config)`

Initialize the SDK exactly once at app entry. Subsequent calls are no-ops.

<CodeBlock lang="ts" code={`import { Sankofa } from "@sankofa/browser";

await Sankofa.init({
  apiKey: process.env.NEXT_PUBLIC_SANKOFA_KEY!,
  endpoint: "https://api.sankofa.dev",
});`} />

### Common options

<ParameterTable
  params={[
    { name: "apiKey", type: "string", required: true, description: "Project API key. The engine resolves environment (live/test) from the key." },
    { name: "endpoint", type: "string", required: true, description: "Server base URL." },
    { name: "debug", type: "boolean", default: "false", description: "Verbose logging during development." },
    { name: "trackLifecycleEvents", type: "boolean", description: "Auto-track $app_opened and visibility transitions." },
    { name: "flushIntervalMs", type: "number", description: "How often the queued events are uploaded while the tab is foregrounded." },
    { name: "batchSize", type: "number", description: "Maximum events per upload batch." },
    { name: "plugins", type: "Plugin[]", description: "Ordered list of plugin instances (catchPlugin, switchPlugin, etc.) to register at init." },
    { name: "release", type: "string", description: "Build identifier — used by Catch for source-map matching." },
    { name: "environment", type: "string", description: "Free-form label (development, staging, production). Tagged onto every event." },
  ]}
/>

`init` returns a `Promise` that resolves once initial setup completes. You don't need to `await` it for `track`/`identify` — those queue synchronously.

## `Sankofa.track(event, properties?)`

Records an event.

<CodeBlock lang="ts" code={`Sankofa.track("checkout_started", {
  cart_value: 49.99,
  item_count: 3,
  currency: "USD",
});`} />

### Parameters

<ParameterTable
  params={[
    { name: "event", type: "string", required: true, description: "Event name. snake_case, present tense, verb-noun. See [naming guide](/concepts/events)." },
    { name: "properties", type: "Record<string, unknown>", description: "Custom properties. Strings, numbers, booleans, arrays, and JSON objects supported." },
  ]}
/>

## `Sankofa.screen(name, properties?)`

Tags the current screen / route. Auto-fires `$screen_view` and stamps the screen name on subsequent events until the next `screen()` call.

<CodeBlock lang="ts" code={`Sankofa.screen("Checkout", { step: "review" });`} />

For SPAs with client-side routing, call `screen` from your route-change effect.

## `Sankofa.identify(userId)`

Sets the active distinct ID. The previous anonymous ID is auto-aliased onto the new user.

<CodeBlock lang="ts" code={`Sankofa.identify("user_123");`} />

If you want to update profile traits at the same time, follow with `peopleSet`:

<CodeBlock lang="ts" code={`Sankofa.identify("user_123");
Sankofa.peopleSet({ email: "ada@example.com", plan: "pro" });`} />

## `Sankofa.peopleSet(traits)` / `setPerson(...)`

Updates the user's People profile **without** changing identity.

<CodeBlock lang="ts" code={`// peopleSet — verbose, accepts any traits object
Sankofa.peopleSet({
  plan: "growth",
  signup_source: "referral",
});

// setPerson — named common traits + a properties bag
Sankofa.setPerson({
  name: "Ada Lovelace",
  email: "ada@example.com",
  avatar: "https://...",
  properties: { plan: "pro", signup_date: "2025-01-12" },
});`} />

Both update the same People profile. Use `peopleSet` when your traits are arbitrary; use `setPerson` when you want the named common traits to surface in the dashboard's People view.

## `Sankofa.reset()`

Rotates the session, clears the active identity, and generates a fresh anonymous ID. Call this on logout.

<CodeBlock lang="ts" code={`Sankofa.reset();`} />

After `reset`, subsequent events use a new anonymous `distinct_id`. Decision handshakes refire to fetch flags / configs for the new identity.

## `Sankofa.flush()`

Drains the queue immediately. Returns a `Promise` that resolves when in-flight requests complete.

<CodeBlock lang="ts" code={`await Sankofa.flush();`} />

Most apps don't need this — the queue flushes automatically on schedule and on `pagehide`. Useful before navigations that bypass the lifecycle hook.

## `Sankofa.shutdown()`

Disposes the SDK — flushes pending events and detaches lifecycle listeners. Use it for tests or when your app needs to fully tear down.

<CodeBlock lang="ts" code={`await Sankofa.shutdown();`} />

## Inspect SDK state

The browser SDK exposes a few read-only accessors useful for debugging or for your own analytics that want to attach the same identifiers:

<CodeBlock lang="ts" code={`// Snapshot of the current decision/flag/config payload at this instant.
const snapshot = Sankofa.getSnapshot();

// Transport health — useful for status banners ("offline", "syncing", etc).
const status = Sankofa.getTransportStatus();
//   { state: "online" | "offline" | "syncing" | "error", lastSuccessAt: number }

const unsub = Sankofa.onTransportStatus((status) => {
  if (status.state === "offline") showOfflineBanner();
});`} />

## Lifecycle integration

The SDK auto-tracks lifecycle events alongside your custom events:

| Event | When |
|---|---|
| `$pageview` | Tab loads, `pushState`, `replaceState`, `popstate`. |
| `$session_start` | First event of a session. |
| `$session_end` | Internal — fires on `pagehide`. Replays and Pulse consume this. |

## What's next

<CardGrid cols={3}>
  <Card href="/sdks/web/packages/catch" eyebrow="Layer on" title="@sankofa/catch">
    Add error capture and breadcrumbs.
  </Card>
  <Card href="/sdks/web/packages/switch" eyebrow="Layer on" title="@sankofa/switch">
    Add feature flags.
  </Card>
  <Card href="/sdks/web/packages/react" eyebrow="React" title="@sankofa/react">
    Use the provider + hooks for declarative integration.
  </Card>
</CardGrid>

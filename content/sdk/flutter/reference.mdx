---
title: Flutter API reference
description: Reference for the public Sankofa Flutter methods and the endpoint normalization helpers used by the SDK.
section: sdk
order: 25
toc: true
badge: Official
source_of_truth:
  - sdk/sankofa_flutter/lib/sankofa_flutter.dart
---

## Core instance methods

<MethodSignature
  name="init"
  signature={`Future<void> init({
  required String apiKey,
  required String endpoint,
  bool debug = false,
  bool trackLifecycleEvents = true,
})`}
  description="Initializes the SDK, resolves endpoint URLs, loads persisted identity and queue state, and gathers default properties."
/>

<MethodSignature
  name="track"
  signature={`Future<void> track(
  String eventName, [
  Map<String, dynamic>? properties,
])`}
  description="Refreshes the current session, serializes event properties, injects $session_id, adds default properties, and appends the event to the local queue."
/>

<MethodSignature
  name="identify"
  signature={`Future<void> identify(String userId)`}
  description="Switches the active distinct ID to a known user and queues an alias item when the previous identity differs."
/>

<MethodSignature
  name="peopleSet"
  signature={`Future<void> peopleSet(Map<String, dynamic> properties)`}
  description="Queues a people profile update for the active distinct ID using the transport serializer."
/>

<MethodSignature
  name="reset"
  signature={`Future<void> reset()`}
  description="Flushes pending events, clears the current user ID, creates a new anonymous ID, and rotates the session."
/>

## Endpoint helpers

<MethodSignature
  name="resolveServerBaseUri"
  signature={`static Uri resolveServerBaseUri(String endpoint)`}
  description="Normalizes an endpoint string back to the engine base URL, even when the input points at /api/v1 or /api/v1/track."
/>

<MethodSignature
  name="resolveTrackUri"
  signature={`static Uri resolveTrackUri(String endpoint)`}
  description="Normalizes the final track URL so the SDK always targets the v1 track endpoint."
/>

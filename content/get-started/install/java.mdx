---
title: Install on Java
description: Add the Sankofa Java SDK (sankofa-catch on Maven Central) to your JVM project. Server-side error tracking with a Servlet filter for HTTP context.
badge: 5 min
toc: true
sdks: [java]
---

The Sankofa Java SDK targets server-side JVM workloads. The artifact is `dev.sankofa:sankofa-catch` on Maven Central and ships error capture, transactions, vitals, and a drop-in Servlet filter.

<Callout tone="note" title="Server-side scope">
The Java SDK focuses on **Catch** (error tracking) — there's no `Sankofa.flags()` or `Sankofa.config()` API today, no Spring Boot starter, and no `@SankofaFlag` / `@SankofaCapture` annotations. To attach the user's flag context to a server-side error, pass `readFlagSnapshot` / `readConfigSnapshot` callbacks at init time.
</Callout>

## Requirements

- **JDK 11+** (we test 11, 17, 21)
- **Gradle 8+** or **Maven 3.9+**

## 1. Add the dependency

<Tabs defaultValue="kts">
  <Tab value="kts" label="Gradle (Kotlin DSL)">
    <CodeBlock lang="kotlin" filename="build.gradle.kts" code={`dependencies {
    implementation("dev.sankofa:sankofa-catch:0.1.0")
}`} />
  </Tab>
  <Tab value="groovy" label="Gradle (Groovy)">
    <CodeBlock lang="groovy" filename="build.gradle" code={`dependencies {
    implementation 'dev.sankofa:sankofa-catch:0.1.0'
}`} />
  </Tab>
  <Tab value="maven" label="Maven">
    <CodeBlock lang="xml" filename="pom.xml" code={`<dependency>
    <groupId>dev.sankofa</groupId>
    <artifactId>sankofa-catch</artifactId>
    <version>0.1.0</version>
</dependency>`} />
  </Tab>
</Tabs>

Make sure Maven Central is in your repositories (default for new projects).

## 2. Initialize the SDK

<CodeBlock lang="java" filename="App.java" code={`import dev.sankofa.sankofacatch.SankofaCatch;
import dev.sankofa.sankofacatch.Options;

public class App {
    public static void main(String[] args) {
        SankofaCatch.init(Options.builder()
            .apiKey(System.getenv("SANKOFA_KEY"))
            .endpoint("https://api.sankofa.dev")
            .environment(System.getenv().getOrDefault("ENV", "production"))
            .release(System.getenv("RELEASE_SHA"))
            .build());

        Runtime.getRuntime().addShutdownHook(new Thread(() ->
            SankofaCatch.flush(2000)
        ));

        // ... start the server
    }
}`} />

For a Servlet container, register `dev.sankofa.sankofacatch.servlet.SankofaCatchFilter` in `web.xml` or via a `ServletContextListener`:

<CodeBlock lang="java" filename="WebInit.java" code={`import dev.sankofa.sankofacatch.servlet.SankofaCatchFilter;
import jakarta.servlet.ServletContextEvent;
import jakarta.servlet.ServletContextListener;

public class WebInit implements ServletContextListener {
    @Override
    public void contextInitialized(ServletContextEvent sce) {
        SankofaCatch.init(/* ... */);
        sce.getServletContext()
            .addFilter("sankofa", SankofaCatchFilter.class)
            .addMappingForUrlPatterns(null, true, "/*");
    }
}`} />

### Common config options

<ParameterTable
  params={[
    { name: "apiKey", type: "String", required: true, description: "Your project's server-side API key." },
    { name: "endpoint", type: "String", required: true, description: "Server base URL." },
    { name: "release", type: "String", description: "Commit SHA or version tag — used by Catch for source-map matching." },
    { name: "environment", type: "String", default: "production", description: "Free-form label (development, staging, production)." },
    { name: "appVersion", type: "String", description: "Your app's user-facing version string." },
    { name: "tracesSampleRate", type: "double", default: "0.1", description: "Distributed-tracing sample rate (0.0–1.0)." },
  ]}
/>

## 3. Verify the install

<CodeBlock lang="java" code={`SankofaCatch.captureMessage("install_check");`} />

Build and run, then open [app.sankofa.dev](https://app.sankofa.dev) → **Live events**. The message should appear within seconds with a `java` source label and the JVM version captured under default properties.

## What's next

<CardGrid cols={3}>
  <Card href="/get-started/first-event" eyebrow="Next" title="Send your first event">
    Multi-platform first-event walkthrough.
  </Card>
  <Card href="/sdks/java/overview" eyebrow="Reference" title="Java SDK reference">
    Capture, breadcrumbs, transactions, and the Servlet filter.
  </Card>
  <Card href="/products/catch/overview" eyebrow="Catch" title="Catch product">
    Server-side error tracking, breadcrumbs, source-map upload.
  </Card>
</CardGrid>
